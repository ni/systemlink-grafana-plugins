# SystemLink Work Items data source

This is a plugin for Work Items from the Work item service. It allows you to:

- Visualize work items metadata and count on a dashboard
- Query selected work item properties or the total count of matching work items
- Filter results with the query builder, select work item types, and configure sort order and result limits
