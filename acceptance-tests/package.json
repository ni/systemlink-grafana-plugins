{
  "name": "systemlink-grafana-plugins-acceptance-tests",
  "main": "fake-api/server.js",
  "type": "module",
  "scripts": {
    "test": "playwright test --pass-with-no-tests",
    "test:debug": "playwright test --headed --debug",
    "test:ci": "cross-env CI=1 playwright test",
    "playwright:setup": "playwright install --with-deps chromium",
    "playwright:setup:ci": "playwright install chromium",
    "lint": "npm run lint:acceptance --prefix ..",
    "lint:fix": "npm run lint:acceptance --prefix .. -- --fix"
  },
  "dependencies": {
    "playwright": "^1.56.1",
    "@playwright/test": "^1.56.1",
    "json-server": "^0.17.4",
    "cross-env": "^10.1.0"
  }
}
